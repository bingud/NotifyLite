<Window x:Class="NotifyLite.Windows.HistoryWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notifications"
        Width="400" SizeToContent="Height"
        MaxHeight="560"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Deactivated="Window_Deactivated"
        Loaded="Window_Loaded"
        ShowActivated="True">

    <Border CornerRadius="3"
            Background="#1F1F1F"
            BorderBrush="#3D3D3D"
            BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.35" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel Grid.Row="0" Margin="16,12,12,8">
                <TextBlock Text="Notifications"
                           FontSize="18" FontWeight="SemiBold"
                           Foreground="#FFFFFF"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left"/>

                <Button x:Name="ClearAllBtn"
                        Content="Clear All"
                        FontSize="16"
                        Foreground="#888"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Padding="8,4"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right"
                        Click="ClearAll_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="2" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#333333"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </DockPanel>

            <!-- Separator -->
            <Border Grid.Row="0" Height="1" Background="#3D3D3D"
                    VerticalAlignment="Bottom" Margin="12,0"/>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          Margin="4,4,4,8"
                          PreviewMouseWheel="UIElement_OnPreviewMouseWheel">
                <StackPanel x:Name="NotificationList"/>
            </ScrollViewer>

            <!-- Empty state -->
            <TextBlock x:Name="EmptyText"
                       Grid.Row="1"
                       Text="No notifications"
                       FontSize="16"
                       Foreground="#888888"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="0,30,0,30"
                       Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
